name: Docker

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    uses: finki-hub/.github/.github/workflows/docker.yaml@main
    with:
      dockerfile: "./local-mcp/Dockerfile"
      context: "./local-mcp"
    secrets: inherit
